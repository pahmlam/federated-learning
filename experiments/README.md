# Experiments

Experiment organization area.

Expected future groups:

- `centralized/`: pooled-data baselines.
- `local_only/`: per-client or per-site baselines.
- `federated/`: Flower/FedAvg and later FL strategies.
- `ablations/`: comparisons for adapters, LoRA, personalization, FedBN, client selection, and communication reduction.

This directory should contain experiment notes/config references, not heavy outputs.

