# Configs

Configuration files will live here when experiments become concrete.

Expected future groups:

- `datasets/`: dataset paths, label schema, client partition rules.
- `experiments/`: run settings for centralized, local-only, and federated experiments.
- `flower/`: Flower server/client/strategy settings.

No YAML/TOML experiment config is created yet.

